# Belajar Web Development Dasar

<!-- absolute url => http://youtube.com => menggunakan hyperlink external (url diluar projek) -->
<!-- relative url => index.html => menggunakan hyperlink internal (masih dalam satu projek) -->

macam - macam operator pada javascript :

# Aritmatika
menggunakan 
# Penugasan
menggunakan tanda =, +=, -=, *=, /=, dan %=
x = y
x += y -> x = x + y
x -= y -> x = x - y
x *= y -> x = x * y 
x /= y -> x = x / y
x %= y -> x = x % y
# Perbandingan
membandingkan sebuah nilai yang hanya ada opesator perbandingan akan menghasilkan nilai true dan false, ada beberapa jenis operator perbandingan :

==  -> digunakan untuk membandingkan nilai
!=  -> 
=== -> digunakan untuk membandingankan nilai beserta tipe datanya
!== -> 
>   ->
<   ->
>=  ->
<=  -> 

# Logika
menggunakan Operator && (AND), || (OR), ! (NOT)

# String


# TYypeof
untuk mengetahui tide data pada suatu nilai variable
# Kondisional


